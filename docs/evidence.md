# Verification evidence

This index separates implementation checks, user-reported observations and recorded native runs. It describes evidence retained in this repository and its local reports, not the current running server. The supported target is TIA Portal V20. A passed engineering workflow does not establish PLC runtime behavior or every native object variant.

| Area | Recorded evidence | Remaining limits |
|---|---|---|
| Connections and project guards | User-executed same-path reopen rejection, detach and discovery checks; the user also confirmed Open project in TIA. See [connection evidence](../reference/history/connection-prototype.md#manual-test-results--2026-09-21) and [dashboard evidence](../reference/history/dashboard-verification.md). | Scenario-specific user reports, not an independently replayed guarantee for every project transition. The [original guard checklist](../reference/history/connection-contract-verification.md) retains its unchecked cases. |
| Block inventory and reads | User-reported inventory comparison and SCL/LAD/DB best-format selection; [original block evidence](../reference/history/block-reader-verification.md). Later import runs verify source checksums and stated semantic assertions for their fixtures. | No exhaustive attribute, mixed-language, protected/safety-object, software-unit or native failure-cleanup coverage. |
| UDT inventory and reads | User-confirmed discovery, best-source and metadata-only workflow; [original UDT evidence](../reference/history/udt-reader-verification.md). Recorded native runs separately passed explicit UDT SimaticML and SIMATIC SD export/import/readback and external-source creation/replacement. | Dependency export and unusual unit/protection/failure cases remain unverified. The early workflow report alone did not verify each exported field or checksum. |
| Typed tag-table entries | User-supplied FIO response: 15 tags with distinct native IDs and metadata-only behavior; [original table evidence](../reference/history/tag-table-reader-verification.md). Later native fixtures verified populated **user-constant** values, IDs, edits and deletion. | Populated **system constants** remain unverified. Empty system-constant arrays do not establish populated field/ID support. |
| Original native acceptance | All nineteen tools in the original scenario set were called across linked runs. User constants and explicit UDT formats passed. The final report remained **incomplete** because SCL FC SIMATIC SD export was unavailable; [run record](../reference/history/native-acceptance-runs.md). | Its unchanged XML/SD round trips establish the stated preservation checks, not semantic changes in those formats. Do not relabel the original report as a complete all-format pass. |
| Native import matrix | Final run passed **14 creation and 14 semantic-update cells**, including LAD SD imports, external SCL/STL/DB/UDT, XML and populated-table imports. The user reported successful compilation; later metadata reads found all 13 retained block/UDT fixtures consistent. See [matrix run record](../reference/history/native-import-matrix-runs.md). | Root compositions and authored fixture semantics only. No claim for every CPU/instruction/type, instance DB or OB replacement, nonempty translated resources, non-root destinations or partial-failure recovery. |
| Compilation, deletion and table export | Agent-executed native success/error/repair compilation, populated-table XML export/import, block/UDT/table deletion with inventory and old-ID absence checks, and successful final compilation. See [lifecycle run record](../reference/history/native-lifecycle-runs.md). | The original report-file failure and reviewed completion remain separate and linked. No force-rebuild, compiler-history or arbitrary XML synchronization guarantee. |
| Cross-references | Initial Level meter query was agent-replayed and user-confirmed against TIA. The expanded V20 run passed repeated accesses, a user constant, call chains, nested DB members, declaration relationships and freshness after edits/deletion; [original evidence](../reference/history/cross-reference-verification.md), [expanded run](../reference/history/native-cross-reference-runs.md). | Repeated native SCL location labels are retained, not treated as distinct source lines. Special safety/protection and software-unit cases remain outside the evidence. |

## Local and transport checks

The Release build checks compatibility with the installed API. The Siemens-free .NET harness exercises production managed contracts, guards and service scheduling. JavaScript tests cover dashboard behavior, architecture boundaries, native-runner fixtures and assertions. The HTTP smoke checks exercise the existing endpoint with passive/disconnected requests; they do not establish native engineering semantics.

Dated build, reload and browser observations remain in [architecture verification](../reference/history/architecture-verification.md), [dashboard verification](../reference/history/dashboard-verification.md) and [write integration verification](../reference/history/write-integration-verification.md). Their test counts, process IDs and attachment state describe the recorded checkpoint only. Current build/check commands are in the [repository README](../README.md).

The [codebase and documentation cleanup checkpoint](../reference/history/cleanup-verification.md) records protocol validation, authenticated lifecycle identity, removal of redundant dashboard routes, the managed reload and its passive HTTP checks. It does not add native project-operation coverage.

## Reports and reruns

Native reports are retained locally under ignored `test-results/` directories because they contain project paths, native identities and complete source/requests/responses. The linked records identify run directories, failures, continuations and evidence limits. Their absence in another checkout is not proof that a run passed or failed.

Active runner instructions are in [native acceptance](native-acceptance.md), [the import matrix](native-import-matrix.md), [lifecycle acceptance](compile-delete-export.md#native-acceptance) and [cross-reference acceptance](native-cross-reference-acceptance.md). A new run needs an authorized disposable target already connected by the user. Existing evidence does not authorize another native write, compile, connection transition or replay. Repeat passed checks when a regression concern or an explicitly selected new scenario justifies it.
