# Historical reference material

Nothing under `reference/` is an active implementation instruction, future-work authorization or runtime fallback. Use [current documentation](../docs/README.md) for current scope and behavior.

## V1 snapshot

`legacy-v1/` preserves the 72 tracked source, test, documentation, editor-task and lifecycle-helper files selected from commit `8ebc151` before the 2026-09-21 source transition. No build outputs, local lifecycle token/state or TIA project files were copied. Source/test files include the then-current connection prototype so the complete baseline remains understandable.

This is comparison material only. Do not build/run its server, tests, mutating editor tasks or lifecycle helper as part of active work. Instructions and capability claims inside this snapshot are historical. The active project and harness have no dependency on it.

The active foundation retains the guarded connection/discovery implementation and shared STA/WinForms host. V1 engineering services and dispatch were retired. The intermediate eight-disabled-descriptor publication hold has also ended; current publication is defined by the active read and write contracts.

## Completed phase records

[Historical handoffs and evidence](history/README.md) preserve the connection prototype, source transition, read-only cutover and retired write probes. Their old startup commands, publication restrictions and continuation prompts apply only to their recorded phase.
